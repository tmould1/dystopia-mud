# Dystopia MUD — Server Update Pipeline Configuration
#
# Change REPO to your fork's owner/repo to pull releases from your own builds.

# GitHub repository (owner/repo)
REPO="terin678/dystopia-mud"

# Release asset filename to download
ASSET_NAME="dystopia-linux-amd64.tar.gz"

# Optional: GitHub personal access token for higher API rate limits
# Unauthenticated: 60 requests/hour. Authenticated: 5,000 requests/hour.
# GITHUB_TOKEN=""
